const bolsaMulher = [
    'cartão de crédito',
    'chave',
    'dinheiro',
    'escova de cabelo',
    'lenço de papel',
    'perfume',
    'alcool-gel'
]

for (let index = 0; index < bolsaMulher.length; index++) {
    const element = bolsaMulher[index]
    console.log(index, element)
    // or
    //console.log(`${index}. ${bolsaMulher[index]}`)
}

